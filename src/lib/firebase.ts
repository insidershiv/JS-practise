/**
 * Firebase + Google Auth — placeholder for future integration.
 *
 * Planned:
 * - Firebase Auth (Google sign-in)
 * - Firestore: users/{uid}/progress (completed question IDs, drafts, streaks)
 *
 * Until then, progress is stored in localStorage (see PracticeWorkspace).
 */

export const FIREBASE_READY = false;
